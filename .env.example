# App Configuration
# Set to true to enable payments, false for free app
ENABLE_PAYMENTS=false

# Keep all other Stripe keys (they just won't be used when payments are disabled)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Stripe keys (not used when ENABLE_PAYMENTS=false)
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_SUBSCRIPTION=
STRIPE_PRICE_EXPORT_PACK=

# License keys (not used when ENABLE_PAYMENTS=false)
LICENSE_PRIVATE_KEY=
NEXT_PUBLIC_LICENSE_PUBLIC_KEY=
